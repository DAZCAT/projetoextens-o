name: Deploy Expo Web to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install deps
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Build Expo Web
        run: npx expo export --platform web

      # Ajuste do base path para o nome EXATO do repositório: Projetoextens-o
      - name: Patch base path for GitHub Pages
        run: |
          sed -i 's|href="/|href="/Projetoextens-o/|g' dist/index.html || true
          sed -i 's|src="/|src="/Projetoextens-o/|g'  dist/index.html || true
          sed -i 's|<head>|<head><base href="/Projetoextens-o/">|g' dist/index.html || true

      # SPA fallback (evita 404 em navegação client-side)
      - name: Copy 404.html for SPA fallback
        run: cp dist/index.html dist/404.html

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: '${{ secrets.GITHUB_TOKEN }}'
          publish_dir: dist
